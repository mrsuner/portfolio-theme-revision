{
  "name": "portfolio-theme-revision",
  "version": "1.1.12-fork.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch --mode development",
    "check": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "vite": "8.3.0"
  }
}
